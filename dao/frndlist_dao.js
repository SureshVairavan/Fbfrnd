/*var friendmodel= require('../models/friendlist_model');

module.exports.getall_friends = function(callback) {
  friendmodel.find(function(err, user) {
    if (err) {
      res.send(err);
    }
    callback(user);
  });
};
*/